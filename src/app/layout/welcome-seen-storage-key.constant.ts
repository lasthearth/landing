/**
 * Ключ хранения признака того, что пользователь уже видел приветственный экран.
 *
 * Используется {@link LocalStorageService}: при первом пролистывании welcome
 * флаг сохраняется, и на последующих визитах экран больше не показывается.
 */
export const WELCOME_SEEN_STORAGE_KEY = 'lh_welcome_seen';
